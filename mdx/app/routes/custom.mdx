---
meta:
  title: Custom MDX Styles
---

import { components } from "~/components/mdx-components";

# Heading

This is some MDX with custom components

Include a `components` variable with the overrides:

```ts
import { components } from "~/components/mdx-components";
```
